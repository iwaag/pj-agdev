# ex2 Step 5 report — failure handling

Not exercised. Test 1 succeeded on the first attempt, so no evidence-driven
correction was applied: zero added guidance lines, zero session.sh allowlist
changes, zero retries.

The only failure-adjacent signal — 11 permission denials inside session 28
that the agent routed around — is recorded as an observation in
`report4.md`; it caused friction, not failure, so under the plan's
one-correction-per-observed-failure rule nothing was changed.
