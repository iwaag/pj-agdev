# Step 4 report — agent-facing documentation

Updated `agent/GUIDE.md`, `agent/README.md`, and the root `README.md` with short
descriptions of the ignored per-project `agents.toml` file, its `coding` and
`director` meanings, job linkage, direction-workspace discovery, and coding
precedence.

The text describes an available developer preference layer. It adds no agent
prompt, mandatory procedure, or repository-enforced selection policy.
