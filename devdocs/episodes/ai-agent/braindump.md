ユーザーをアシストするエージェントとのやりとりをできるようにしたい。
例によって最低限から。最初はテキスト入力とテキスト表示でいい。
右の方にAIのアバターとのチャット領域を表示。よくあるAIエージェントの会話パネル風に。
最初は取得済みのクラスタ情報を読んで、クラスタのノードについて質問に答えたり、ノードの状態を説明する、というタスクだけできるようにする。
その次は画面をdesired nodeの一覧とdesired workspaceの一覧画面を切り替える、みたいなフロントエンド側の操作をさせるようにしたいが、今回の作業には含めない。

エージェントをどう動かすかはまだ考えていない。既存のollamaサーバー、node-agent用に各ノードに置いてるopencode、cluster-agent、もしくはpj-clusterintentのnctlのexecuterみたいな超小型エンジンを自作するか。サーバー側で接続するのか、フロントエンド側で接続するのかも気になる。