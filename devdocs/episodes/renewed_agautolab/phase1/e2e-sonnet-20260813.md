# Phase 1 E2E on the `sonnet` front profile (2026-08-13)

Status: **pass — the full path completed on the first attempt**

Re-run of the phase 1 end-to-end check after two changes and a profile switch.
Nothing was deployed to agautolab1.

## Configuration for this run

- `.local/agents.local.toml` gained `[roles.front] profile = "sonnet"`
  (claude_code + `anthropic/claude-sonnet-5`). The tracked `agents.toml`
  default is still `local`.
- `absolute_dump_notice()` absolutises the chat-log path handed to `/window`
  — see [warning.md](../warning.md); it is a workaround, not design.
- `echo*` added to the front and mediator opencode permissions and
  `Bash(echo:*)` to `WORKING_ALLOWED_TOOLS`.
- Both launchd services restarted so they ran the new code.

## Result

Created `#pj-e2e-sonnet-20260813` with the autolab bot unsubscribed, then wrote
one request to `mission-readme-sonnet` (message `#93`).

| Stage | Result |
|---|---|
| periodic `pj-*` discovery and subscription | pass — subscribed 49 s after channel creation, no listener restart |
| acceptance of the `mission-*` topic | pass — `#93` at `05:49:44Z` |
| `topic_dump` | pass — version `1` under the front workspace |
| `init_project.py` | pass — Plane `E2e Sonnet 20260813` (`ES20260813`), Gitea `e2e-sonnet-20260813` and `-direction`, both clones |
| gateway `/window` → front | pass — `run-0008`, `outcome: done`, 4 turns, 9.5 s, $0.103 |
| `new_mission.py` | pass — Plane issue `Draft the project README`, state `Todo` |
| `topic_write` | pass — reply `#94` in the original topic |

The front reply reached Zulip unchanged:

```text
Mission registered successfully: **"Draft the project README"** — write the
initial README.md describing the project's purpose and local run instructions.
```

No retry, no path clarification, no failure farming this time.

## Comparison with the `local` run

The same workflow on `local` (report `e2e-recheck-20260813.md`) failed at the
front stage: the model corrupted the chat-log path, produced no final text, and
the listener aborted with a 502 before `topic_write`. `sonnet` completed in
4 turns; the cost is $0.103 per mission topic instead of $0.

## Residue

`#pj-e2e-sonnet-20260813`, Plane project `ES20260813` with one issue, two Gitea
repositories, and two ignored clones were left in place. The front profile
override is still in `.local/agents.local.toml`; revert it to return the front
to the local model.
